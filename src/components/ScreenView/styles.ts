import { StyleSheet } from 'react-native';

import COLORS from '../../theme/colors';

export default StyleSheet.create({
    screen: {
        flex: 1,
        backgroundColor: COLORS.screenDefault,
    },
});
