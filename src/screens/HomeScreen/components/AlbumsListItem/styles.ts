import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        height: 50,
        justifyContent: 'flex-start',
        marginBottom: 10,
    },
    number: {
        fontWeight: 'bold',
    },
    image: {
        width: 50,
        height: '100%',
        marginHorizontal: 10,
    },
    noImageText: {
        marginHorizontal: 10,
        color: '#CCCCCC',
    },
});
